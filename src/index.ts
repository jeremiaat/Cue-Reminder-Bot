// Vercel Functions entrypoint marker.
// This project is serverless (api/webhook.ts + api/cron.ts); it has no
// long-running process. This file exists so Vercel's Node builder finds
// an entrypoint instead of failing with "No entrypoint found".